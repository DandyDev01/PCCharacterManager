<UserControl x:Class="PCCharacterManager.Views.ListViewItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:local="clr-namespace:PCCharacterManager.Views"
             d:DesignHeight="50" d:DesignWidth="175" MinWidth="100" 
             BorderThickness="0,5,0,0" BorderBrush="White">
    <Grid>
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Add" Command="{Binding}"/>
                <MenuItem Header="Remove" Command="{Binding}"/>
            </ContextMenu>
        </Grid.ContextMenu>
        <TextBlock Style="{StaticResource h2}" Text="{Binding }" TextWrapping="WrapWithOverflow"/>
    </Grid>
</UserControl>
