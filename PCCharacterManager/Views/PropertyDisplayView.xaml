<UserControl x:Class="PCCharacterManager.Views.PropertyDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PCCharacterManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="200" MaxWidth="350"
             BorderBrush="White" BorderThickness="0,2,0,0">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" Grid.RowSpan="1"
               Content="{Binding Name, FallbackValue=Name}"
               Style="{StaticResource mediumn}"/>

        <TextBlock Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Grid.RowSpan="1"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   FontSize="13"
                   Text="{Binding Desc}"/>

    </Grid>
</UserControl>
