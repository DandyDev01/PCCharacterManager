<UserControl x:Class="PCCharacterManager.Views.StringListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PCCharacterManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="1" Grid.RowSpan="1"
                   Text="{Binding ListName}"/>
        <ListView Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="1" Grid.RowSpan="1"
                  SelectedItem="{Binding SelectedItem}"
                  ItemsSource="{Binding ItemsToDisplay}">
            <ListView.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Remove item" Command="{Binding RemoveItemCommand}"/>
                    <MenuItem Header="Edit item" Command="{Binding EditItemCommand}"/>
                    <MenuItem Header="Add item" Command="{Binding AddItemCommand}"/>
                </ContextMenu>
            </ListView.ContextMenu>
        </ListView>
    </Grid>
</UserControl>
