<UserControl x:Class="PCCharacterManager.Views.NoteDisplayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PCCharacterManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300" MaxHeight="150">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border BorderBrush="{StaticResource LightText}" BorderThickness="2"/>
        <Label Grid.Row="0"
               Content="{Binding Title}"/>

        <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="6" Grid.RowSpan="1"
                BorderBrush="{StaticResource LightText}"
                BorderThickness="1"
                Padding="5">
            <TextBlock Style="{StaticResource h4}"
                       Visibility="{Binding IsDisplayMode, Converter={StaticResource IsEditConverter}}" 
                       Text="{Binding Notes, FallbackValue=info}"/>
        </Border>

    </Grid>
</UserControl>
